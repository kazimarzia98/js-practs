// if want to swap number then use temp


let first = 5;
let second = 7;

console.log(first, second);

//  use temp to swap number,means first number will be secnd nmbr ,secnd nmbr will be first nmbr 
// temp means temporary

const temp = first;
first = second;
second = temp;
console.log(first, second);